# 문서 위치 지도

> **어떤 문서가 어디에 있는지** 안내하는 지도다 — "무엇이 존재하는가"를 빠짐없이 세는 장부가 아니다.
> 새 문서는 아래 분류에 맞는 위치에 두고, 이 지도를 갱신한다. 폴더·파일명·지도가 분류를 대신하므로 모든 파일을 등재할 필요는 없다.

---

## 프로젝트 운영

권위 구조의 정의는 작업 규약 §0에 있다. 본 지도는 위치만 안내한다.

| 문서 | 위치 | 내용 |
|---|---|---|
| 작업 규약 | `docs/manual/working_protocol.md` | **작업 방법론의 단일 정본** — 권위 구조·역할 분담·승인 목록·문서 규칙·Git 규칙·세 작업 루프 |
| 프로젝트 지침 | `AGENTS.md` | 매 세션 로드되는 작동 발췌본 + **프로젝트 고유·동적 정보의 정본**(§1) |
| 도구 어댑터 | `CLAUDE.md` | `@AGENTS.md` 한 줄. 규칙을 담지 않는다 |
| 거시 로드맵 | `docs/manual/roadmap.md` | 마일스톤 목록과 운용 제약의 정본. 현재 단계는 `AGENTS.md` §1 |
| 문서 위치 지도 | `docs/document_map.md` | 본 문서 (문서가 어디 있는가) |

주제별 발췌본(「브랜치 전략」·「문서 체계」 등)은 두지 않는다 — 정본의 해당 절을 읽는다(규약 §0 ④).

## 타스크 산출물

계획서는 `docs/plans/`, 단계별 완료보고서는 `docs/working/`, 최종 결과보고서는 `docs/report/`, 오늘 할 일은 `docs/orders/`에 둔다. 파일명 규칙은 작업 규약 §4를 따르며, **개별 타스크 산출물은 본 지도에 등재하지 않는다** — 파일명이 곧 색인이다.

**구상·설계 논의는 `docs/topics/` 의 논의 스레드에 쌓고, 결론은 그 스레드의 색인이 정본이다.** 타스크 산출물은 결론을 인용한다.

## 논의 스레드

`docs/topics/NNN_주제명/` 하나가 스레드 하나다. **목록 파일을 따로 두지 않는다** — 폴더가 곧 목록이다. 상태는 이렇게 훑는다.

```sh
grep -H '^- \*\*상태\*\*' docs/topics/*/000_색인.md
```

| 스레드 | 주제 |
|---|---|
| `docs/topics/001_프로젝트_경쟁력/` | 이 도구가 현존 임상 도구와 겨루어 만들 가치가 있는가 |

## 설계·도메인 자산

| 문서 | 위치 | 상태 |
|---|---|---|
| 자발화 분석 도구 현황 조사 | `docs/domain/language_sample_analysis_tools.md` | 2026-08-23 조사. 이 분야 도구들의 작동 방식과 이 도구의 위치 |
| 실전 실패 원인 진단 | `docs/tech/field_failure_diagnosis.md` | 2026-08-23 첫 실전 적용 실패의 수치 규명. `v0.1` 의 출발점 |

## 코드와 검증

| 대상 | 위치 | 내용 |
|---|---|---|
| 본체 | `src/speech_index.py` | 오디오 추출 → 발성 검출 → 화자 판별 → 전사 → 산출물 생성 |
| 리뷰어 화면 | `src/reviewer_template.html` | 데이터를 주입해 `reviewer.html` 로 산출되는 단일 HTML |
| 사용법 | `docs/manual/usage.md` | 옵션·운용 절차의 정본 |
| 회귀 검증 | `tests/regression.py` | 합성 음원으로 검출률·분류율·오탐비 측정 |

## 여기 없는 것

**실제 회기의 음성·영상과 그 산출물**은 저장소에 없다 — 금지선이다(`AGENTS.md` §2). 검증에는 `tests/regression.py` 가 만드는 합성 음원만 쓴다.

**방법론 규칙의 정본이 유지되는 곳**은 프로젝트 생성 템플릿이다. 여기 있는 정본은 그 복제본이며, 개선하면 템플릿에 반영한다(규약 §6).
