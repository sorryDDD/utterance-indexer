# 2026-08-23

## 완료 — 프로젝트 독립

`clinical-practicum` 안에서 만들던 도구를 별도 프로젝트로 분리했다. 개발 규모가 커져 임상 실습
프로젝트 안에서 관리하기 어려워졌다는 판단에 따른 것이다.

- 프로젝트 생성 템플릿에서 복제하고 `AGENTS.md` §1·§2를 기입했다.
- 코드를 `src/`, 사용법을 `docs/manual/usage.md`, 조사 자료를 `docs/domain/` 에 배치했다.
- 임시로 쓰던 검증 절차를 `tests/regression.py` 로 정식 편입했다. 자동 검증 명령이 실제로 작동한다.
- 교재 병행 절(`AGENTS.md` §6)은 해당 없어 삭제했다.

**옮겨온 것의 원본은 아직 `clinical-practicum` 에 남아 있다.** 삭제는 승인 사항이다.

## 다음 — 실전 실패 원인 규명 (`v0.1`)

2026-08-23 실제 회기 영상(약 1시간 45분, `.mov`)에 적용한 결과 보고된 증상:

- 임상가와 대상자의 발화를 **거의 구분하지 못한다.**
- 리뷰어에서 **전사된 발화 단 하나가 모든 구간에 반복된다.**

합성 음원 회귀 검증은 검출·분류 모두 100%로 통과하므로, **검증이 실전 조건을 대변하지 못하고
있다.** 원인 규명이 먼저이고, 그 뒤에 검증을 실전 조건에 맞춰 고친다.

산출물이 크므로(구간 수천 개) 전체를 훑지 않고 **분석 구간을 선별**한다.

> 다음 작업의 계획서는 원인 규명 착수 시점에 연다.
