# 2026-08-25

## 경량 — 산문 하드랩 금지

문서 형식 규칙에 **하드랩 금지**를 신설하고 기존 문서에 소급 적용했다. 계기는 타스크 #1 계획서를 쓰며 관례를 잘못 읽은 것이었다.

- 규약 §4 「문서 형식」이 줄바꿈을 규정하지 않는 사이, AI가 쓴 문서들이 표시폭 ~95칸 하드랩으로 수렴해 있었다. 템플릿 유래 문서(문단 단위 줄바꿈)와 갈리는 선이 정확히 "AI가 새로 쓴 문서"였고, 드리프트가 `AGENTS.md`(70~100칸대 0% → 23%)와 `README.md`(9% → 53%)까지 번져 있었다.
- 폭이 아니라 **행위**를 금지했다. 폭을 정하면 그 값이 또 역추론 대상이 되지만, 하드랩을 금지하면 고를 값이 없다.
- 소급 적용은 Codex에 위임했다 — 추적 문서 20개에서 문단 118개·목록 항목 28개. 공백류를 제거한 문자열이 20개 파일 모두 수정 전후 동일함을 확인해 내용 무변경을 검증했다.
- 린트 설정 주석이 "문장 단위로 한 줄"이라 새 규칙(문단 단위)과 어긋나 함께 고쳤다. 전역 베이스 `~/Workspace/.markdownlint-base.jsonc`와 프로젝트 생성 템플릿에도 같은 개정을 반영했다.

커밋 — `d2ac0f3` 규칙 명시 · `c456770` 소급 적용 · `6f3c821` 린트 주석 정정.

## 진행 중 — v0.1 정답 라벨 확보 경로 (Issue #1)

이날 타스크 #1을 착수해 계획서를 두 차례 고치고 단계 1을 구현·검증했다. **그 진행분은 `20260828.md` 에 함께 정리했다** — 8/25 착수부터 8/28 승인·커밋까지가 끊기지 않고 읽히도록 한자리에 모았고, 각 절에 실제 날짜를 달아 두었다.
