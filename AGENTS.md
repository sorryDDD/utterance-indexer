# 프로젝트 지침

> 본 문서는 두 가지를 담는다. ① 작업 규약(`docs/manual/working_protocol.md`)에서 **행동을 직접 규율하는 규칙·절차만 뽑은 작동 발췌본**, ② 이 프로젝트에 **고유한 정보**. ②는 본 문서가 정본이다(규약 §0).
> 방법론 규칙이 규약과 어긋나면 규약이 우선한다. 규칙을 새로 정하거나 고칠 때는 규약을 먼저 고치고 본 문서의 해당 발췌를 같은 커밋에서 맞춘다.
> 〔기입〕은 프로젝트 개시 시 채운다. 인용문 절은 자리 예약이다.

## 1. 이 프로젝트

**본 절이 프로젝트 고유 정보의 유일한 자리다.** 매뉴얼 문서에는 이 내용을 옮겨 적지 않는다.

### 목표와 요구사항

언어재활·진단 임상 회기를 녹화한 긴 영상에서 **누가 언제 소리를 냈는지**를 자동으로 찾아내고, 임상가가 그 지점 사이를 건너뛰며 들을 수 있게 하는 로컬 전용 도구다. 자발화 분석의 표집 단계에서 발화를 찾느라 소모되는 시간을 줄이는 것이 유일한 목적이다.

- 전 과정 **로컬 처리**. 임상 음성·영상·전사 결과는 어떤 경로로도 외부에 나가지 않는다.
- 발성 검출은 **재현율 최우선**. 놓친 발화는 복구할 수 없고, 오탐은 리뷰어에서 한 번 눌러 넘긴다.
- **화자 판별**로 대상자 구간과 임상가 구간을 가른다.
- 1~2시간 회기 영상을 실용적 시간 안에 처리한다.
- 주 산출물은 **점프 탐색이 가능한 리뷰어**, 부 산출물은 타임코드 표와 자막이다.
- **임상 보편성을 염두에 둔다.** 특정 대상자·기관에 맞춘 도구가 아니다. 설계가 특정 조건 — 대상자의 협조, 특정 장비, 특정 녹음 환경 — 을 전제하면 그 전제가 서지 않는 현장에서 통째로 무용해진다. 예컨대 **대상자에게 마이크를 착용시키는 방식은 전제로 삼을 수 없다** — 착용을 거부하거나 감각 문제로 불가능한 경우가 필시 있다. 임상가 쪽에 거는 조건은 그렇지 않다.

**일반화 금지선**

- 이 도구는 **전사기가 아니다.** 전사는 맥락 파악용 보조이며 품질을 목표로 삼지 않는다.
- **자발화 분석의 임상적 판정을 대신하지 않는다.** 무엇이 발화인지, 그 발화가 무엇을 뜻하는지는 임상가가 듣고 판단한다.
- **진단·평가 도구가 아니다.** 지표를 산출하더라도 그것은 탐색 보조 정보다.

### 아키텍처·기술 스택

확정일 2026-08-23.

| 층 | 채택 | 비고 |
|---|---|---|
| 실행 | Python 3.14 (`.venv`) | 표준 라이브러리 + numpy |
| 수치 연산 | numpy 2.5 | 유일한 파이썬 의존성 |
| 미디어 입출력 | ffmpeg / ffprobe (Homebrew) | 컨테이너·코덱 제약 없음 |
| 발성 검출 | 자체 에너지 VAD + Silero VAD (`whisper-vad-speech-segments`) | 두 결과의 합집합 |
| 전사(보조) | whisper.cpp `whisper-cli` + `ggml-large-v3-turbo-q5_0` | Metal 가속 |
| 화자 판별 | 자체 구현 — 로그 스펙트럼 32밴드 + F0, k-means | 외부 모델 없음 |
| 리뷰어 | 의존성 없는 단일 HTML + 순수 JS | 로컬 파일 또는 내장 `http.server` |

**범위 전제 — 포기한 것**

- **PyTorch 기반 스택**(pyannote, Voice Type Classifier 등)을 도입하지 않는다. 설치 부담과 실행 환경 복잡도를 피하고 numpy만으로 성립하는 범위를 유지한다. **이 결정은 v0.1 결과에 따라 재검토 대상이다.**
- **클라우드 ASR·외부 API**를 쓰지 않는다(금지선).
- **GUI 앱**으로 만들지 않는다. 산출물은 브라우저에서 여는 정적 HTML이다.

### 현재 상태

- **현재 로드맵 단계**: 미정 (마일스톤 목록의 정본은 `docs/manual/roadmap.md` — `v0.1` 항목이 아직 〔기입〕이다)
- **현재 가동 마일스톤**: `v0.1`

프로토타입이 동작하고 합성 음원 회귀 검증은 통과한다(검출·분류 100%). 그러나 2026-08-23 실제 회기 영상(1시간 45분)에 적용한 결과 **화자 판별이 거의 작동하지 않았고 전사 결과도 비정상**이었다. 합성 검증과 실전 성능의 괴리를 규명하는 것이 v0.1의 핵심 문제다.

**정답 라벨을 확보하고 현행·수정 후 파이프라인을 평가했다**(타스크 #1·#2, 2026-08-29). 같은 회기의 12.9분에서 사람이 표시한 대상자 발성 84개를 기준으로 합집합은 84/84를 검출했지만 현행 화자 판별은 대상자 후보 23/69(33.3%), 임상가 발성의 대상자 후보 혼입 63/165(38.2%)였다. F0 진단을 보강하고 대상자 무리를 총 발화 시간으로 고르자 후보 성공은 51/69(73.9%)로 올랐지만 임상가 발성의 대상자 후보 혼입도 104/165(63.0%)로 늘었다. 개선의 직접 원인은 F0가 아니라 무리 선택이었고 화자 판별 문제는 미해결이다. 단일 편향 표본이므로 다른 회기로 일반화하거나 임계를 튜닝하지 않으며 추가 라벨·홀드아웃 검증을 우선한다. 절차는 `docs/manual/labeling_procedure.md`, 표본 한계와 결과는 `docs/report/task_m010_1_report.md`·`task_m010_2_report.md`를 보라.

### 빌드·실행과 자동 검증

명령은 모두 **프로젝트 루트**에서 실행한다.

**최초 준비**

```sh
python3 -m venv .venv && ./.venv/bin/pip install numpy
brew install ffmpeg whisper-cpp
mkdir -p ~/.cache/whisper.cpp && cd ~/.cache/whisper.cpp
curl -LO https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-large-v3-turbo-q5_0.bin
curl -LO https://huggingface.co/ggml-org/whisper-vad/resolve/main/ggml-silero-v5.1.2.bin
```

**실행**

```sh
./.venv/bin/python src/speech_index.py <영상경로> --serve
```

**자동 검증**

- 문서 변경: `markdownlint-cli2 '**/*.md' '!.venv'` 통과.
- 코드 변경: `./.venv/bin/python tests/regression.py` 통과(약 5초). 합성 음원으로 검출률·화자 분류율·오탐비를 재고 기준 미달이면 종료 코드 1을 낸다.

**검증의 한계** — 회귀 검증은 합성 음원만 쓴다(금지선). **이것을 통과해도 실전 성능은 보장되지 않는다.** 실제 회기 영상 검증은 작업지시자가 수행하고 결과를 `docs/feedback/`에 남긴다.

### 원격 저장소와 공개 범위

- **원격 저장소**: [sorryDDD/utterance-indexer](https://github.com/sorryDDD/utterance-indexer) (`origin`).
- **공개 범위**: `private`. 저장소 소유자와 직접 초대된 collaborator만 접근한다.
- 기본 브랜치는 `main`, 통합 브랜치는 `devel`이다.
- 커밋 메시지는 한국어로 쓴다.
- **추적 예외**: 산출물 폴더(`speech-index-*`), 산출물 파일, 모든 미디어 파일, `.venv/`. `.gitignore` 참조.

## 2. 승인 없이 진행하지 않는 작업

아래 작업은 작업지시자의 명시적 승인 없이 진행하지 않는다.

1. **커밋·push**
2. **기술 스택·아키텍처 결정**
3. **프로젝트 데이터의 외부 전송·게시** — 저장소나 작업물의 내용이 밖으로 나가는 경우. 기술 정보 조회와 소프트웨어·모델 내려받기는 받아오는 방향이므로 승인 없이 한다. 조회 요청에 프로젝트 데이터를 실어 보내는 것은 여기 해당하며, 금지선에도 걸린다
4. **파일 삭제·덮어쓰기** — 특히 AI 에이전트가 직접 만들지 않은 파일
5. **저장소 공개 범위 변경** (private → public)
6. **로드맵 단계 추가·삭제·순서 변경**
7. **★금지선** — 임상 음성·영상·전사 결과와 대상자 식별 정보를 저장소에 넣거나 외부로 내보내는 일
8. **★사람만 내리는 판정** — 어떤 구간이 실제 대상자 발화인지, 그것이 임상적으로 무엇을 뜻하는지

①에 상설 승인 예외를 둔다면 그 범위도 여기 적는다.

이 목록은 작업 루프 등급과 무관하게 발효한다. 위 항목이 걸렸다고 루프를 통째로 올리지 않고, 걸린 그 항목의 관문만 얹는다.

### 금지선

다음은 저장소에 들어가지 않는다. 어느 브랜치에도, 저장소가 로컬 전용이어도 예외가 없으며, `docs/orders/*`·커밋 메시지·코드 주석까지 저장소에 들어가는 모든 텍스트에 걸린다.

- 대상자의 실명·생년월일·연락처 등 **식별 정보**. 필요하면 "대상자"로만 쓴다.
- 실제 회기의 **음성·영상 파일과 그 산출물** — `speech-index-*/`, `audio.wav`, `asr.json`, `segments.json`, `subtitles.vtt`, `reviewer.html` 등.
- 실제 회기의 **전사 내용**. 버그 재현에 필요하더라도 원문을 옮기지 않고 현상만 기술한다.
- 특정 대상자의 **진단명·임상 소견**.

**대신 쓰는 것** — 검증은 `tests/regression.py` 가 만드는 합성 음원만 쓴다. 실전에서 발견한 문제는 조건(길이·화자 수·잡음 상황)과 증상만 적는다.

**외부 전송 금지** — 임상 음성·전사 내용과 대상자 정보를 클라우드 ASR·외부 API·원격 저장소로 보내지 않는다. 웹 검색어나 외부 도구의 프롬프트에 싣는 것도 같다. 이 도구가 로컬 전용인 이유가 그것이며, 승인으로 열리지 않는다.

반대 방향은 막지 않는다 — 기술 문헌 조회, 패키지·모델 내려받기는 승인 없이 한다(§2 ③). 받아온 모델은 로컬에서만 돌리며, 처리할 음성을 그 서비스로 보내지 않는다.

### 도메인 특수 항목

AI 에이전트는 검출·정렬·집계와 그 결과의 **초안 분석까지만** 하고, 다음 판정은 하지 않는다.

- **어떤 구간이 실제 대상자 발화인가** — 도구가 내는 것은 후보일 뿐이다. 특히 비언어적 발성, 웅얼거림, 자기자극적 소리를 '발화'로 볼지는 임상적 판단이다.
- **자발화 분석 결과의 해석** — 산출된 발화 수·길이·빈도가 대상자의 언어 능력에 관해 무엇을 말하는지.
- **도구 출력의 임상적 유효성** — 실습·평가에 실제로 쓸 수 있는 수준인지.

## 3. 문서 규칙

- 본 문서에는 행동을 직접 규율하는 규칙·절차와 §1의 프로젝트 고유 정보만 쓴다. 취지·논증·경위는 규약에 쓰고, **고유 규칙은 매뉴얼 문서로 옮기지 않는다.**
- **산문은 하드랩하지 않는다** — 문단은 줄바꿈 없이 한 줄로 쓴다. 줄바꿈은 문단·목록 항목·표 행의 경계에서만 넣는다.

### 문서 폴더 구조와 역할

| 폴더 | 용도 |
|---|---|
| `docs/orders/` | 오늘 할 일 (`yyyymmdd.md`) |
| `docs/plans/` | 작업·실행계획서 |
| `docs/working/` | 단계별 완료보고서 |
| `docs/report/` | 최종 결과보고서 |
| `docs/feedback/` | 사람의 검토·교정 기록 |
| `docs/tech/` | 기술적 발견 |
| `docs/troubleshootings/` | 문제·해결 경험담 |
| `docs/manual/` | 지속 참조 매뉴얼 — 작업 방법론 정본의 위치 |
| `docs/topics/` | 논의 스레드 — 구상·설계 논의 (`NNN_주제명/`) |
| `docs/design/` | 설계 문서 (독자 분류) |
| `docs/domain/` | 도메인 지식 자료 (독자 분류) |

빈 폴더는 `.gitkeep`으로 git에 등록한다.

### 파일·폴더 명명 규칙

- 일반 **MD 문서 파일명은 snake_case**. 단 도구·생태계가 규약으로 강제하는 문서(`AGENTS.md`·`CLAUDE.md`·`README.md` 등)는 예외.
- **폴더명은 항상 kebab-case**. 단 논의 스레드 폴더는 예외다.
- **논의 스레드는 번호 접두를 쓰고 한글 이름을 허용한다** — 폴더 `NNN_주제명/`, 색인 `000_색인.md`, 글 `NN_제목.md`.
- 코드·컴포넌트·설정파일 등 그 외 유형은 규칙 대상이 아니며 각 생태계 관례를 따른다.
- 타스크 산출물 파일명: 작업계획서 `task_m010_2.md`, 실행계획서 `task_m010_2_exec.md`, 단계별 완료보고서 `task_m010_2_stage1.md`, 최종 결과보고서 `task_m010_2_report.md`. 실행계획서와 단계별 완료보고서는 중량 작업 루프 전용이다.
- 파일명의 마일스톤은 생략된 patch를 `0`으로 보충한 소문자 `m{SemVer}`로 축약한다(`v0.1` → `m010`). GitHub 마일스톤·orders 표제는 버전 문자열을 그대로 쓴다.

### 문서 운용

- 새 문서는 분류에 맞는 폴더에 두고 **문서 위치 지도**(`docs/document_map.md`)를 갱신한다. 전 산출물을 단일 목록에 등재하지 않는다.
- **구상·설계 논의는 `docs/topics/` 의 논의 스레드에 쌓는다.** 조사·검토·반론을 독립 문서로 흩지 않는다.
- **논의의 결론은 그 스레드의 색인(`000_색인.md`)이 정본이다.** 타스크 산출물은 인용만 한다.
- **폐기된 스레드는 폴더명 끝에 `--폐기됨`을 붙인다**(`012_주제명--폐기됨/`) — 폴더 목록이 곧 스레드 목록이므로 상태가 이름에 드러나야 한다. 주제명이 언더스코어를 쓰므로 구분자는 이중 하이픈이다. 번호는 재사용하지 않는다.
- 스레드 규칙 — 색인은 「무엇을 논의하는가 · 현재 논의 상태 · 미해결 · 오간 주장 · 결론」 다섯 절. 글은 머리에 `작성일 · 작성자 · 근거`, 말미에 `## 주장` 절(번호 붙인 한 문장, "~하여야 한다"). 근거는 `실측 / 문헌 / 추론 / 미확인`.
- **반박도 새 글이다. 기존 글은 고치지 않는다**(오탈자·링크 제외). 번호는 재사용·재배열하지 않고, 인용은 `001#02` 꼴로 번호로 한다.

## 4. 검증과 품질

- **자동 검증**은 AI 에이전트가 수행하고 결과를 보고한다. 명령은 §1 「빌드·실행과 자동 검증」에 있다.
- **작업지시자 판정**은 대신하지 않는다 — 시각적 품질·도메인 적합성처럼 사람 판단이 필요한 결과는 환경·시나리오·자동 결과만 준비하고 판정을 넘긴다.
- 계획서에는 둘의 대상·방법·판정 기준을 구분해 적는다.

## 5. 워크플로우

### 브랜치 관리

| 층 | 브랜치 | 용도 |
|---|---|---|
| 위 | `main` | 릴리스. 항상 배포 가능한 상태 유지, 릴리스 시 태그 |
| 가운데 | `devel` | 여러 타스크의 통합·검증 |
| 아래 | `local/task{N}` | 타스크별 격리 작업 (`N` = GitHub Issue 번호) |

이슈를 채번하지 않는 경량 작업은 `local/{슬러그}`를 쓴다(예: `local/fix-toolbar-label`). 진행 중 상위 루프로 승격하면 이슈를 만든 뒤 `git branch -m local/{슬러그} local/task{N}`으로 바꾼다.

### Git 워크플로우

- 흐름: 타스크 브랜치 → (관련 타스크 묶어서) `devel` merge → (릴리스 시점) `main` merge (+태그).
- **커밋은 마감 승인 뒤, push는 반영 승인 뒤에** 한다.
- 커밋 메시지는 "무엇을·왜"를 요약한다. **이슈가 있으면 `Task #N:` 접두어를 붙이고, 없으면 붙이지 않는다.**
- **커밋 메시지에 `closes #N`·`fixes #N` 등 이슈 자동 종료 키워드를 쓰지 않는다.** Issue 종료는 반영 승인 뒤 원격 반영을 확인하고 나서 수행하는 별도 절차다.

### 타스크 관리 — 이슈·마일스톤·오늘할일

- 중량·일반 작업 루프의 타스크는 **GitHub Issue로 채번**한다 — 이슈 번호가 곧 타스크 번호이자 브랜치 번호다. 경량 작업 루프는 채번하지 않는다.
- GitHub 마일스톤과 orders 표제는 **버전 문자열로 명명**한다(`v0.1` 등, `M##` 축약 없음). 타스크 산출물 파일명에만 소문자 `m` 축약을 쓴다. 빈 미래 마일스톤은 미리 만들지 않는다.
- `docs/orders/yyyymmdd.md`로 하루를 여닫는다. 마일스톤 절 표제는 버전 문자열로 쓴다(예: `## v0.1 — …`).
- 현재 가동 마일스톤은 §1 「현재 상태」에 있다.

### 타스크 진행 절차 — 세 작업 루프

작업 루프는 **복잡도**로 가른다. 지표는 착수 시점에 답을 모르는 질문의 수이며, 판정은 착수 시점에 하고 그 질문들을 근거로 남긴다. 애매하면 상위 루프가 기본값이다.

| 작업 루프 | 부르는 조건 | 문서 | 관문 |
|---|---|---|---|
| **중량** | 미지가 많다 — 중간 결과를 봐야 다음 단계가 정해진다 | 작업계획서 + 실행계획서 + 단계별 완료보고서 + 최종 결과보고서 | 작업계획 · 실행계획 · 단계별 N회 · 마감 · 반영 |
| **일반** | 미지가 적다 — 구현하며 밝혀지고 방향은 바뀌지 않는다 | 작업계획서 1종(무엇·왜·어떻게) + 최종 결과보고서 | 계획 · 마감 · 반영 |
| **경량** | 미지가 없다 — 무엇을 어떻게 할지 확정되어 있다 | 계획서 없음 — orders 한 줄 | 마감 · 반영 |

절차는 아홉 단계이며 루프에 따라 일부가 빠진다.

| # | 단계 | 중량 | 일반 | 경량 |
|---|---|---|---|---|
| 1 | Issue 채번 — 범위·마일스톤·루프 판정·모르는 질문 | ● | ● | 없음 |
| 2 | 타스크 브랜치 생성 | `local/task{N}` | `local/task{N}` | `local/{슬러그}` |
| 3 | 작업계획서(무엇·왜) → **〈작업계획 승인〉** | ● | 어떻게까지 담아 **〈계획 승인〉** | 없음 |
| 4 | 실행계획서(어떻게·단계 분할) → **〈실행계획 승인〉** | ● | 없음 | 없음 |
| 5 | 구현·자동 검증 | 승인된 단계만 | 전 범위 한 번에 | 전 범위 한 번에 |
| 6 | 단계별 완료보고서 → **〈단계 승인〉** → 커밋 → 5 반복 | ● | 없음 | 없음 |
| 7 | 최종 결과보고서 + 완료일 orders | ● | ● | orders 한 줄 |
| 8 | **〈마감 승인〉** → 커밋 → `devel` 통합 → 회귀 검증 | ● | ● | ● |
| 9 | **〈반영 승인〉** → push → 원격 확인 → Issue 종료 → 브랜치 삭제 | ● | ● | ● (Issue 없음) |

- 4의 단계는 중간 확인이 필요한 지점의 수만큼 나눈다(최소 단계 수 없음). 자동 검증과 작업지시자 판정을 구분해 적는다.
- 단계 승인(6)은 그 단계의 커밋까지만이고 마감·반영을 포함하지 않는다. 8·9에는 정지 조건을 두지 않는다 — 오류가 나면 그 자리에서 판단해 대응하고 보고한다. `main` 통합·태그, 저장소 공개 범위 변경, 외부 전송은 묶음 밖의 별도 관문이다.
- **승격**은 진행 중 새 질문이 드러나면 AI가 그 자리에서 하고, **강등**은 작업지시자만 결정한다. 상위 루프가 요구하는 문서를 새로 쓰되 이미 만든 변경은 그 현황으로 흡수하고, 강등은 이미 만든 이슈·브랜치·문서를 지우지 않는다.
- 식별자는 바꾸지 않는다. 유일한 예외가 경량에서 이슈가 처음 생기는 순간의 `git branch -m`이다. 승격 전 커밋의 접두어는 소급해 고치지 않으며, 이미 통합·push된 작업의 후속은 승격이 아니라 새 타스크다.
- 수정 요청·반려 시 같은 단계에서 수정·재검증·재보고하고 다시 승인받는다.

## 6. 핵심 문서 위치

| 문서 | 위치 |
|---|---|
| 작업 규약 — 작업 방법론의 단일 정본 | `docs/manual/working_protocol.md` |
| 거시 로드맵 — 마일스톤 목록의 정본 | `docs/manual/roadmap.md` |
| 문서 위치 지도 — 문서가 어디 있는가 | `docs/document_map.md` |
