.DS_Store

# `.markdownlint.jsonc`는 추적 대상이다 — 문서 형식 규칙을 강제하는 유일한 장치이므로
# 저장소에 함께 남긴다(작업 규약 §4 「문서 형식」).

# 〔기입〕 스택이 정해지면 해당 생태계의 무시 규칙을 여기 더한다.
# 예) node_modules/ · dist/ · .env · __pycache__/ · .venv/
