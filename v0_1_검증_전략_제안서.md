# 통제된 사람 모사 회기로 v0.1 검증 경로를 세운다

- **작성일**: 2026-08-31
- **작성자**: Codex (작업지시자의 제안을 팀 공유용으로 정리)
- **근거**: 배경으로 인용한 수치는 실측과 문헌이다. 그러나 **모사 자료로 경로를 고를 수 있다는 제안 자체는 추론**이며, 그 추론이 깨지는 조건은 §9에 적었다.

> **문서 상태**: SNS에서 팀 합의를 구하기 위해 프로젝트 루트에 임시로 둔 **미승인 제안**이다. 정식 논의 스레드나 프로젝트 정본이 아니며, v0.1 로드맵, 아키텍처, 열린 Issue의 범위·순서 또는 구현을 바꾸지 않는다. 팀 의견을 모은 뒤 작업지시자가 채택해야만 결정과 타스크로 넘어간다.

## 한눈에 보는 제안

이 프로젝트는 긴 임상 회기 녹화본에서 **누가 언제 소리를 냈는지**를 찾아 임상가가 후보 사이를 건너뛰며 원음을 검토하게 하는 로컬 전용 도구를 만들고 있다. 합성 음원에서는 검출·분류 100%였지만 첫 실제 회기에서 화자 판별이 거의 작동하지 않았고, 정답 라벨로 다시 잰 뒤에도 문제는 해결되지 않았다. 이제 병목이 화자 판별이라는 데까지는 도달했으나, 정답 라벨을 갖춘 실제 회기 자료가 한 회기의 편향된 12.94분뿐이어서 어떤 경로를 선택해야 하는지 비교할 근거가 부족하다.

이에 **두 명 이상의 프로젝트 진행자가 치료실과 음향 조건이 유사한 공간에서 임상가 역할과 대상자 역할을 바꾸어 맡고, 발성 길이·음량·거리·중첩·잡음 조건을 의도적으로 다양화한 사람 모사 회기 자료를 만든다**고 제안한다. 이 자료를 개발용과 **모사 홀드아웃**(결과를 볼 때까지 열지 않고 봉해 두는 자료)으로 먼저 나눈 뒤, 현행 단일 원거리 채널을 기준선으로 두고 §4에 벌여 둔 나머지 경로들을 같은 자료·같은 채점 기준으로 비교한다. v0.1의 목적은 실제 임상 유효성을 입증하는 것이 아니라 **다음 주 경로를 근거 있게 선택하는 것**으로 둔다.

팀이 지금 판단할 것은 상세 실험 설계가 아니다. 이 목표를 v0.1로 채택할지, v0.1 이전의 예비실험으로만 둘지, 참여·타당성 문제로 보류할지를 먼저 정해야 한다.

## 1. 프로젝트가 해결하려는 문제

긴 회기에서 자발화 표본을 찾으려면 임상가는 영상 전체를 들으며 대상자의 소리가 나오는 지점을 찾아야 한다. 이 프로젝트는 그 임상 판단을 대신하지 않고 **들을 후보와 타임코드를 넓게 모아 탐색 시간을 줄이는 것**만 목표로 한다. 전사기는 아니며, 실제 발화 여부와 의미는 임상가가 원음을 듣고 판단한다.

현재의 개념 흐름은 `회기 녹화본 → 사람 발성 후보 검출 → 누가 낸 발성인지 추정 → 모든 후보를 원본에 연결한 리뷰어 → 임상가 판단`이다. 발성 후보는 놓치면 뒤에서 복구할 수 없으므로 **재현율**(놓치지 않는 비율)을 우선하고, 중첩·제3자·미확정은 억지로 대상자나 임상가 한쪽에 넣지 않고 보존하는 방향을 논의해 왔다. 구체 장비와 알고리즘을 정하지 않은 흐름도 초안은 [개념 처리 흐름도](conceptual_processing_flow.md)에 있다. 이 문서는 그 셋째 칸 — 「누가 낸 발성인지 추정」하는 일 — 을 시종 **화자 판별**로 부른다. pyannote 같은 기성 제품이 **화자 분리**(diarization)라 부르는 것과 같은 일이며, 이름만 다르다.

변하지 않는 제약은 셋이다.

- 임상 음성·영상·전사와 대상자 정보는 외부로 보내지 않고 전 과정을 로컬에서 처리한다.
- 대상자에게 마이크 착용을 요구하는 방식을 보편 경로의 전제로 삼지 않는다. 착용 거부·감각 문제·주의 분산이 있는 대상자에게는 그 전제가 서지 않기 때문이다.
- 기계가 만드는 것은 `발성 후보`와 화자 `추정`이다. 실제 발화 여부와 임상적 의미는 사람이 판단한다.

## 2. 지금까지 무엇을 했고 무엇이 드러났는가

아래 표와 수치에는 프로젝트 안에서만 통하는 낱말이 나온다. 뜻만 먼저 적어 둔다.

| 낱말 | 뜻 |
|---|---|
| **합집합** | 두 검출기가 잡은 구간을 합친 것. 하나라도 잡으면 후보로 남긴다 |
| **에너지 VAD** · **Silero** | 앞은 소리 크기만 보고 사람 소리를 찍는 자체 구현, 뒤는 학습된 음성 검출 모델. 이 둘의 합집합이 현행 기본 경로다 |
| **F0** | 목소리의 높이 |
| **무리** · **k-means** | 뒤는 비슷한 것끼리 자동으로 묶는 방법, 앞은 그렇게 묶인 덩어리. 현행은 덩어리 둘 중 하나를 대상자로 고른다 |
| **분리도** · **%p** | 대상자를 맞힌 비율에서 임상가를 잘못 넣은 비율을 뺀 값. `%p`는 두 비율의 차이를 세는 단위다 |
| **분절** | 소리 구간을 어디서 끊는가 |
| **상류** | 먼저 고쳐야 할 문제 |

| 시점·작업 | 수행한 일 | 드러난 결과 |
|---|---|---|
| 2026-08-23 · 첫 실전 적용 | 합성 회귀검증을 통과한 프로토타입을 105.3분 실제 회기에 실행 | 합성 검출·분류 100%와 달리 화자 판별이 거의 작동하지 않았고 전사도 비정상이었음 |
| 타스크 #1 | 파이프라인 출력과 독립적인 라벨러·절차를 만들고 실제 회기 12.94분을 사람이 라벨링 | 대상자 84개·임상가 180개·보류 3개를 확보했으나, 대상자 발성이 많은 블록을 골랐고 한 회기뿐이라는 선택 편향이 남음 |
| 타스크 #2 | 같은 라벨로 현행 기준선 측정, F0 진단 보강과 대상자 무리 선택 오류 수정, 재측정 | 합집합 검출은 84/84였으나 화자 판별은 수정 후에도 대상자 후보 51/69·임상가 발성의 대상자 후보 혼입 104/165였음 |
| 타스크 #6 | 회기 전체·라벨 블록·검출 구간의 구조를 다시 대조하여 병목 감사 | 「발성 검출의 잡음 통과가 상류」라는 결론을 거두고, 측정 가능한 v0.1 병목을 화자 판별로 확정 |
| 2026-08-23 이후 · 논의 스레드 | 아홉 개 스레드에 조사·검토·반박 글을 쌓아 제품 가치, 실패 원인, 화자 판별 경로와 실행 스택, 녹음 조건과 제3자 혼입, 촬영 중 표식을 검토 | 여러 후보 경로의 장단점과 폐기된 가설이 드러났지만 같은 정답 조건에서 직접 비교하지 못함. 묶음별 결론은 [부록 A](#부록-a--논의가-남긴-설계-결론)에 있다 |

### 실제 라벨이 보여 준 중심 수치

| 설정 | 대상자 검출 | 대상자 후보 성공 | 임상가→대상자 후보 혼입 |
|---|---:|---:|---:|
| 수정 전 합집합 | 84/84 (100%) | 23/69 (33.3%) | 63/165 (38.2%) |
| 수정 후 합집합 | 84/84 (100%) | 51/69 (73.9%) | 104/165 (63.0%) |

**분모가 라벨 개수와 다른 이유부터 밝힌다.** 검출은 대상자 라벨 84개 전부에서 재지만, 화자 판별 성적은 임상가와 조금이라도 겹친 15개를 뺀 **69개**에서 재고, 혼입은 대상자·보류와 겹치지 않은 임상가 발성 **165개**에서 잰다. 한 구간에 두 사람이 함께 들어 있으면 판별기가 무엇을 답하든 옳고 그름을 물을 수 없기 때문이다. 이 둘을 이하 **대상자 후보 성공**과 **임상가 혼입**으로 부른다.

수정 후 대상자 후보 성공이 40.6%p 올랐지만 임상가 혼입도 24.8%p 늘었다. 개선의 직접 원인은 F0가 아니라 **더 큰 무리를 대상자로 고른 것**이었다. **대상자 후보 성공률은 후보를 많이 담을수록 저절로 오른다 — 모든 구간을 후보로 두면 100%가 된다.** 고른 무리가 전체의 37.6%에서 62.6%로 커졌으니 성공률이 오른 것은 당연하다. F0 수정으로 전체 1,760개 중 무리 번호가 달라진 구간은 두 개뿐이었다.

**게다가 임상가 쪽에는 신호가 아예 없다.** 임상가 발성의 63.0%가 대상자 후보로 갔는데, 그 무리가 전체의 62.6%를 차지한다. 두 값이 같다는 것은 **무작위로 골라 담은 것과 구분되지 않는다**는 뜻이다. 따라서 현재 결과를 화자 판별 개선으로 볼 수 없고, 대상자 후보 성공과 임상가 혼입을 반드시 함께 읽어야 한다. 자세한 채점 정의와 한계는 [타스크 #2 결과보고서](docs/report/task_m010_2_report.md)에 있다.

### 발성 검출보다 화자 판별이 먼저인 이유

한때 에너지 VAD가 많은 잡음을 통과시키는 것이 실패의 상류라고 보았다. 그러나 그 근거였던 5분은 회기를 5분씩 나눈 21개 토막 가운데 Silero 검출이 가장 적은 토막이었다. 라벨 블록에서는 합집합이 대상자 84/84를 살렸고, Silero 단독은 65/84만 잡았다. 합집합이 키우는 **후보 부담** — 임상가가 리뷰어에서 눌러 넘겨야 할 후보의 양 — 은 남지만, 현재 표본에서 먼저 줄이면 대상자 발성을 잃는다.

검출 구간을 그대로 둔 채 가장 유리하게 화자를 배정했을 때 가능한 분리도 상한은 +60.6%p였고 현행은 +10.9%p였다. **현행은 상한의 18%를 쓴다.** 분절이 병목이라면 현행이 상한에 붙어 있어야 하는데 그렇지 않으므로, 분절은 당장의 병목이 아니다. 다만 검출 시간의 48%가 두 화자를 함께 포함했고, 구간을 통째로 임상가로 버리면 대상자 라벨 84.5%를 함께 잃는다. 참조 채널을 쓰더라도 결국 **구간 안에서 시간 단위로 다시 가르는 문제**가 남는다. 이 감사와 한계는 [타스크 #6 결과보고서](docs/report/task_m010_6_report.md)에 있다.

### 논의가 남긴 것 — 요지 셋

위 표의 마지막 행은 아홉 스레드를 한 줄로 접은 것이다. 그 안에서 이 제안의 형태를 실제로 정한 것은 네 묶음이며, 각 묶음이 무엇을 묻고 무엇을 폐기했는지는 [부록 A](#부록-a--논의가-남긴-설계-결론)에 있다. 요지만 먼저 적으면 셋이다.

- **화자 판별 엔진은 제품 정체성이 아니라 교체 가능한 부품이다**([001 프로젝트 경쟁력](docs/topics/001_프로젝트_경쟁력/README.md)). v0.1을 자체 알고리즘 교정이 아니라 경로 비교·선택 단계로 두는 이 제안의 형태가 여기서 나왔다.
- **검출 임계**(이 값을 넘으면 사람 소리로 친다는 기준 숫자) **조정과 전사 개선은 첫 행동이 아니다**([002](docs/topics/002_첫_실전_산출물_실패/README.md)·[004](docs/topics/004_발성_검출_설계/README.md)·[005](docs/topics/005_전사_기능의_위치/README.md)). 합집합을 먼저 조이면 대상자 발성을 잃고, 전사의 반복 환각은 도구가 아니라 앞 문장을 다음 구간으로 물고 들어가는 설정(컨텍스트 캐리오버)의 문제였다.
- **문헌·추론만으로는 어느 판별 경로도 승인할 수 없다**([003](docs/topics/003_화자_판별_방식/README.md)·[006](docs/topics/006_실행_스택/README.md)·[008](docs/topics/008_옆방_음성_혼입/README.md)·[009](docs/topics/009_촬영_중_순간_표식/README.md)·[010](docs/topics/010_임상_현장의_녹화_관행/README.md)). 최신 임베딩을 실제로 갈아 끼운 결과는 현행보다 나빴고, 실제 회기에서는 사람이 제3자·중첩의 정답을 만들어 내지 못했다. **통제된 모사 자료가 필요한 가장 직접적인 이유가 이것이다.**

## 3. 현재 해결해야 할 문제를 어떻게 좁혔는가

현재 증거로 고정할 수 있는 문제는 다음과 같다.

> **대상자와 임상가를 갈라내는 방법을 찾아야 한다.** 조건은 셋이다.
>
> - 대상자에게 장비를 채우지 않는다. 멀리 놓인 마이크로 찍은 회기 녹음이 입력이다.
> - 발성 후보는 계속 넓게 잡는다. 지금의 재현율을 깎지 않는다.
> - 임상가 발성이 대상자 후보로 섞여 드는 양을 줄인다.
>
> 그리고 짧은 발성·작은 소리·거리 변화·동시 발화·제3자 음성에서 **어디서 무엇 때문에 실패하는지가 드러나야 한다.**

이 문장은 해결책을 고정하지 않는다. 다만 다음 세 방향은 지금의 첫 행동으로 삼기 어렵다는 점이 드러났다. 앞의 둘은 이미 [결정 장부](docs/decisions.md)에 승인된 선택으로 올라 있고([D-002](docs/decisions.md#d-002-vad-union)·[D-003](docs/decisions.md#d-003-energy-vad-gate)·[D-004](docs/decisions.md#d-004-zero-f0-gate)·[D-009](docs/decisions.md#d-009-speaker-bottleneck)), 셋째는 실측 한 건에 근거한 이 제안의 판단이다.

- **발성 검출 임계를 먼저 조정하는 길**: 현재 라벨에서 합집합은 대상자 발성을 모두 살렸고, 에너지 VAD 단독 표지를 제거하면 재현율이 떨어진다. 다른 자료 없이 임계를 조정하면 한 표본에 대한 튜닝이다.
- **F0 조건을 먼저 다듬는 길**: 이미 한 차례 원리적 오류를 고쳤지만 실제 무리 배정에는 거의 영향을 주지 않았다. 새 근거 없이 남은 게이트를 조정하면 우선순위와 효과가 불명확하다.
- **최신 화자 임베딩으로 교체하면 된다는 길**: 화자 임베딩은 목소리를 숫자 벡터로 바꿔 서로 비교하는 방식이다. 그 대표 격인 ECAPA-TDNN을 넣은 제한된 실측에서는 짧은 구간의 길이에 더 결부되고 무리 분리도도 낮아졌다. 이것이 pyannote 전체 파이프라인을 기각하지는 않지만, 모델 이름만 바꾸어 해결된다고 전제할 수는 없다.

## 4. 지금까지 검토한 경로와 설계

세부 근거와 아직 답하지 못한 질문은 [화자 판별 방식 스레드](docs/topics/003_화자_판별_방식/README.md)와 [임상 현장의 녹화 관행 조사](docs/topics/010_임상_현장의_녹화_관행/01_회기_녹화의_이유와_방식_조사.md)에 모여 있다.

| 경로 | 기대하는 것 | 현재 확인된 한계·미확인 |
|---|---|---|
| 현행 단일 원거리 채널 · 로그 스펙트럼+F0+k-means | 기존 영상에 그대로 적용, numpy 전용 유지 | 실제 무리는 화자보다 음량·길이와 더 결부됐고, 임상가 발성은 무작위로 담은 것과 구분되지 않게 갈림(§2) |
| pyannote 등 기성 화자 분리 파이프라인 | 분절·임베딩·중첩 처리를 묶은 강한 기준선 | 짧고 원거리인 우리 조건에서 미실측, PyTorch·모델 내려받기·실행 복잡도 증가, ECAPA 단독 실측만으로는 우열 판정 불가 |
| 임상가 참조 마이크 · 채널 에너지비(두 마이크에 들어온 소리 크기의 비) | 음색으로 대상을 맞히는 대신 임상가인가 아닌가를 물리적 채널 차이로 측정, 대상자 착용 불필요 | 새 장비·동기·도입 마찰, 대상자·임상가 동시 발화를 프레임 단위로 다시 나눌 방법, 실제 조건의 에너지비가 모두 미검증 |
| 방에 흩은 다중 마이크·기존 관찰실 채널 | 누구에게도 장비를 달지 않고 위치·에너지 차이를 사용 | 두 사람이 가까이 있거나 동시에 말하면 분리도가 약해질 수 있고 실제 치료실에서 미실측 |
| 대상자 근접 채널 | 대상자 음성을 가장 직접적으로 얻음 | 착용 거부·감각·주의 분산이 핵심 사용례와 겹치므로 보편 경로의 전제로 삼을 수 없음. 대상자별 선택적 보조 경로로만 남을 수 있음 |

현장 녹화 관행 조사에서는 붙박이 단일 원거리 마이크 사례와 관찰실 다채널 설비가 함께 확인됐다. 임상가 개인 마이크는 대상자에게 조건을 걸지 않는다는 장점이 있지만 관행 밖의 새 장비 요구일 수 있다. 한국 사설 치료실과 스마트폰 한 대로 찍는 현장의 실제 분포는 아직 확인되지 않았다. 따라서 **실제로 재보기 전에는 다섯 중 무엇도 고를 수 없고, 무엇도 버릴 수 없다.** 이 제안이 모사 자료로 만들려는 것이 바로 다섯을 같은 자리에서 재는 조건이다.

전사는 이 비교의 중심이 아니다. 임상가 참조 채널이 생기면 전사 품질이 나아질 가능성은 있으나, 전사는 계속 맥락 파악용 보조이며 대상자 발화 판단이나 의미 해석을 대신하지 않는다.

## 5. 왜 지금의 열린 일만으로는 다음이 정해지지 않는가

현재 열린 일은 세 조각이며, **셋 다 착수 보류 상태다.**

- [Issue #3](https://github.com/sorryDDD/utterance-indexer/issues/3)은 합성 회귀검증에서 길이·음량과 화자의 완전 상관을 깨려 한다. 필요한 코드 검증이지만 사람 음성·방·거리·중첩을 대표하지 않는다. 이 제안의 §6이 같은 상관을 실제 음성 층에서 깨려는 것이므로 **둘은 같은 목적을 다른 증거 층에서 수행한다**(§6 「증거의 세 층」).
- [Issue #4](https://github.com/sorryDDD/utterance-indexer/issues/4)는 같은 실제 회기의 발화가 드문 블록을 추가 라벨링하려 한다. 기존 선택 편향을 치지만 같은 화자·방·장비를 공유하므로, 어느 입력 조건이 나은가라는 질문에는 구조적으로 답할 수 없다.
- [Issue #5](https://github.com/sorryDDD/utterance-indexer/issues/5)는 F0의 남은 세 조건이 원리적 결함인지 판정하려 한다. 그러나 앞선 F0 수정의 실전 영향이 매우 작아, 화자 판별 경로를 정하기 전에 가장 먼저 할 이유가 약하다.

세 일은 각각 타당하며 이 제안은 그중 무엇도 기각하지 않는다. 다만 셋 중 어느 것도 **어떤 화자 판별 경로를 살릴 것인가**에 답하지 않는다. 그 질문에 먼저 답해야 세 일의 순서가 정해지므로, 보류가 풀리는 차례도 거기에 딸려 있다. [거시 로드맵](docs/manual/roadmap.md)의 v0.1 항목이 아직 비어 있어 종착점 자체가 정해지지 않은 것도 같은 자리다.

## 6. 제안하는 검증 경로

### 자료의 성격

프로젝트 진행자들이 임상가 역할과 대상자 역할을 맡아 실제 사람 음성으로 짧은 모사 회기를 녹음한다. 이것은 현재 회귀검증의 합성 파형보다 실제 음성·발화 길이·방의 울림을 더 잘 담지만, 실제 대상자와 임상 상호작용을 대신하지 않는다. 문서와 지표에서는 `대상자 발화`가 아니라 **`대상자 역할 참여자의 발성`**으로 부른다. 그래서 §2의 두 지표도 모사 자료에서는 **대상자 역할 후보 성공**·**임상가 역할 혼입**이 된다 — 계산은 같고, 실제 대상자가 아니라는 사실만 이름에 남긴다.

프로젝트 진행자의 음성도 개인을 식별할 수 있는 자료다. 녹음 전 참여 동의, 접근자, 저장 위치, 보존·폐기 시점을 합의하고 기본적으로 저장소 밖 로컬 위치에 둔다. 외부 게시나 전송은 이 제안에 포함하지 않는다.

### 반드시 깨야 할 상관

현재 합성 음원은 임상가가 항상 길고 크게, 대상자가 항상 짧고 작게 말하므로 길이·음량만 보아도 화자를 맞힐 수 있다. 사람 모사 자료에서는 한 녹음 안의 역할은 고정하되, 같은 대본과 조건을 별도로 반복할 때 두 참여자의 역할을 맞바꾼다. 두 역할 모두 짧고 긴 발성, 작고 큰 소리, 가깝고 먼 위치를 갖게 한다. 그러면 **어떤 구간이 「길어서」 임상가로 찍힌 것인지 「목소리가 달라서」 찍힌 것인지를 갈라 볼 수 있다.** 지금은 그 둘이 붙어 있어 무엇 때문에 맞고 틀리는지 알 수 없다.

최소 조건표에는 다음 축이 들어간다. 각 축의 구체 수준과 반복 수, 그리고 그 **참여 부담**(사람이 실제로 들이는 시간과 수고)의 견적은 팀 합의 뒤 중량 타스크의 설계에서 정한다.

- 짧은 단순 발성·짧은 말·긴 말
- 작은 소리·보통 소리·큰 소리
- 마이크와의 거리·방향·자리 이동
- 비중첩·부분 중첩·동시 발성
- 웃음·헛기침·클릭음 등 사람이 내는 비언어적 소리와 생활 잡음
- 가능하다면 제3자 역할 또는 외부 음성 조건
- 채널 배치 — 방 마이크 단독, 방에 흩은 다중 마이크, 임상가 역할의 근접 채널, 각 참여자의 정답용 근접 채널

### 한 번의 녹음으로 §4의 다섯 경로를 모두 잰다

이 설계의 이점은 **경로를 미리 고르지 않아도 된다**는 데 있다. 마이크를 함께 세워 녹음해 두면 같은 자료에서 다음이 모두 나온다.

| §4의 경로 | 모사 자료에서 재는 방법 |
|---|---|
| 현행 단일 원거리 채널 | 방 마이크만 파이프라인에 넣는다. 기준선이다 |
| 방에 흩은 다중 마이크 | 흩어 둔 채널들의 위치·에너지 차이만 쓴다. 실제 치료실 미실측이라는 유일한 감점을 여기서 해소한다 |
| 임상가 참조 마이크 | 방 마이크와 임상가 역할의 근접 채널을 함께 넣는다 |
| 대상자 근접 채널 | 제품 입력으로는 주지 않는다. 다만 정답 채널이 곧 이 경로의 성능 상한이다 — **대상자에게 마이크를 채웠을 때 잘해야 어디까지 되는지가 덤으로 같이 나온다** |
| pyannote 등 기성 파이프라인 | 입력은 방 마이크로 같다. 다만 스택 도입이 걸리므로 포함 여부는 팀이 따로 정한다 |

### 입력 채널과 정답 채널을 분리한다

각 참여자의 근접 채널은 누가 언제 소리를 냈는지 시간축을 만드는 **정답 계측 장치**로 쓴다. 어느 경로를 평가하든 파이프라인에 주는 채널은 위 표의 「재는 방법」 칸으로 한정하고, 정답용 근접 채널은 채점에만 쓴다. 대상자 역할의 근접 채널은 어느 평가에서도 제품 입력으로 주지 않는다. 그러면 대상자 착용을 보편 전제로 삼지 않으면서도 중첩 구간의 정답을 만들 수 있다.

### 개발 자료와 홀드아웃을 먼저 나눈다

녹음 계획 단계에서 개발 자료와 열지 않은 모사 홀드아웃을 분리한다. 개발 자료는 실패 조건을 찾고 방식을 수정하는 데 쓰며, 홀드아웃은 알고리즘·임계·채점 기준을 고정한 뒤 한 번만 연다.

참여자가 두 명뿐이면 다른 날·새 대본·새 위치로 나눌 수 있지만 같은 두 화자를 공유하므로 **조건 홀드아웃**이다. 네 명 이상이 참여하여 개발에 쓰지 않은 화자 쌍을 남기면 **화자 홀드아웃**까지 가능하다. 어느 수준이 현실적인지는 팀의 참여 가능성을 먼저 확인해야 한다.

### 증거의 세 층을 섞지 않는다

| 층 | 답하는 질문 | 답하지 못하는 질문 |
|---|---|---|
| 합성 회귀검증 | 코드 변경이 이미 막은 원리적 오류를 다시 만들지 않는가 | 실제 음성·방·화자 조건에서 작동하는가 |
| 통제된 사람 모사 회기 | 조건을 바꿀 때 어느 경로가 무엇 때문에 실패하고, 열지 않은 모사 자료에도 반복되는가 | 실제 대상자·임상 상호작용에 일반화되는가 |
| 향후 실제 회기 외부검증 | 임상 조건에서 후보가 실제 탐색에 쓸 만한가 | 표본이 적을 때 임상 보편성이 입증되는가 |

모사 자료가 좋아도 실제 임상 유효성을 주장하지 않는다. 성인 진행자가 아동·청소년의 음역, 비언어적 발성, 돌발 움직임, 실제 촉진–반응 중첩과 임상적 의미를 재현할 수 없기 때문이다. 모사 검증으로 얻는 것은 둘이다 — **무엇 때문에 실패하는지 가려내는 일, 그리고 어느 경로로 갈지 고르는 일.**

## 7. 제안하는 v0.1 목표와 종료 조건

### 목표 문장

> **v0.1 — 통제된 사람 모사 회기 자료로 반복 가능한 평가 체계를 만들고, §4에 벌여 둔 화자 판별 경로들을 같은 자료·같은 채점 기준으로 재어 어느 것이 살아남는지 판정한다. 실제 임상적 유효성은 아직 주장하지 않는다.**

### 종료 조건

이 목표를 채택한다면 v0.1은 좋은 성능이 나올 때까지 계속하는 단계가 아니라 다음 여섯 조건을 충족하면 끝나는 **경로 선택 마일스톤**이 된다.

1. 팀이 녹음 조건·역할·자료 관리와 사람 판정의 경계를 승인한다.
2. 역할·길이·음량·거리의 완전 상관을 깨는 모사 자료를 만들고 개발 자료와 홀드아웃을 결과 확인 전에 고정한다.
3. 현행 단일 채널 기준선을 대상자 역할 후보 성공, 임상가 역할 혼입, 조건별 실패, 후보 부담으로 측정한다.
4. §4의 나머지 경로를 같은 자료와 채점 기준으로 측정한다. 한 번의 녹음으로 함께 재는 것(다중 마이크·임상가 참조 채널·대상자 근접 채널의 상한)과 스택 도입이 걸려 별도 판단이 필요한 것(pyannote 등 기성 파이프라인)을 나누어 정한다.
5. 홀드아웃을 열기 전에 최소 성능과 감당 가능한 후보 부담의 판정 기준을 동결한다. 임상적으로 감당 가능한 수준은 작업지시자가 판단한다.
6. 결과에 따라 살아남은 경로를 기록한다 — `단일 채널을 주 경로로 계속한다 / 다른 입력 경로를 주 경로로 검토한다 / 어느 쪽도 부족하여 스택·입력 조건 또는 프로젝트 지속 여부를 다시 판단한다` 중 하나다.

어느 경로도 살아남지 못했다는 결론도 완료다. 실패를 숨긴 채 기준 숫자를 이 자료에 맞춰 더 만지작거리는 대신, 다음에 자원을 들일지 말지를 판정할 수 있기 때문이다.

### 포함하지 않는 것

- 실제 임상 유효성 또는 임상 보편성 입증
- 대상자에게 마이크 착용을 요구하는 보편 경로 채택
- 실제 발화 여부·의미·도구의 임상적 유효성을 기계가 판정하는 일
- 모사 홀드아웃 결과를 본 뒤 같은 자료에 맞추어 임계를 다시 조정하는 일
- 팀 합의 전에 로드맵·아키텍처·열린 Issue를 변경하는 일

검토는 했으나 이번 범위에서 뺀 개별 항목은 [부록 A](#부록-a--논의가-남긴-설계-결론) 머리에 있다.

## 8. 팀이 먼저 합의할 항목과 그 뒤의 행동

상세 실험 설계에 들어가기 전에 각 팀원이 다음을 검토해 의견을 남겨야 한다.

1. **목표** — v0.1을 실제 임상 유효성 입증이 아니라 모사 회기 평가 체계와 화자 판별 경로 선택 단계로 두는 데 동의하는가.
2. **참여 가능성** — 누가 녹음에 참여할 수 있고, 역할 교대·반복 녹음·새 대본 홀드아웃이 원리적으로 가능한가. 몇 시간·몇 회라는 견적은 설계 단계의 일이므로 여기서는 가능 여부만 본다.
3. **공간·장비** — 치료실과 유사한 공간, 방 마이크, 다중 마이크 배치, 정답용 근접 채널, 임상가 참조 채널을 어떤 구성으로 확보할 수 있는가.
4. **자료 관리** — 참여자의 음성 자료를 누가 어디서 얼마 동안 보관하고 언제 폐기할 것인가.
5. **증거 한계** — 모사 결과를 실제 대상자 성능이나 임상 유효성으로 일반화하지 않는 경계에 동의하는가.
6. **선택지** — 아래 세 갈래 중 어느 쪽인가.

| 선택 | 뜻 | 다음 행동 |
|---|---|---|
| A · v0.1 목표로 채택 | 모사 평가와 경로 선택을 v0.1의 종착점으로 둠 | 작업지시자 승인 후 로드맵 기입, 새 중량 타스크를 채번해 녹음 설계·자료 분할·채점 기준·단계별 정지 지점을 계획한다. 보류 중인 Issue #3·#4·#5는 그 계획 안에서 순서를 잡는다 |
| B · 예비실험으로만 채택 | 작은 모사 녹음으로 가능성과 참여 부담부터 확인 | v0.1 정의를 건드리지 않는 별도 범위의 예비실험을 계획하고, 그 결과 뒤 v0.1 목표를 다시 논의한다 |
| C · 보류·기각 | 참여·타당성·우선순위 문제가 큼 | 현행 로드맵을 유지하고 보류 중인 Issue #3·#4·#5의 순서를 별도로 정한다 |

**합의 전 지금 할 일은 이 글을 팀에 공유하고 위 여섯 항목의 의견을 모으는 것뿐이다.** 로드맵·결정 장부·아키텍처·코드·Issue 범위는 바꾸지 않으며 녹음을 시작하지 않는다. 이 순서를 지키면 팀은 아직 합의하지 않은 목표를 수행하느라 자원을 쓰지 않으면서도, 합의 뒤에는 "이제 무엇을 해야 하는가"를 다시 처음부터 찾지 않고 곧바로 계획 단계로 넘어갈 수 있다.

## 9. 반대 논거와 정지 조건

이 제안은 실제 자료 부족을 해결하지 않고 **잘 통제된 역할극으로 가릴 위험**이 있다. 성인 진행자의 목소리 차이가 너무 크거나 역할을 과장하면 현행 방식도 쉽게 성공할 수 있다. 같은 팀이 설계·녹음·개발·평가를 모두 맡으면 조건을 무의식적으로 알고 맞출 수 있다. "치료실과 유사하다"는 표현도 방 크기·잔향·배경 소음·마이크 거리 기록이 없으면 인상에 그친다.

따라서 다음 중 하나라도 성립하면 모사 자료를 v0.1의 검증 근거로 부르지 않고 설계를 다시 논의하거나 중단한다.

- §6의 설계 요건 셋 — 역할 교대와 길이·음량·거리 분포의 중첩, 정답 채널과 평가 입력 채널의 분리, 결과를 보기 전의 홀드아웃 고정 — 가운데 하나라도 만들 수 없다.
- 참여자·공간·장비가 너무 적어 조건 변화가 한 화자 쌍이나 한 배치와 완전히 결부된다.
- 팀이 음성 자료 관리와 참여 부담에 합의하지 못한다.
- 모사 성적을 실제 대상자 성능으로 읽어야만 프로젝트 가치를 주장할 수 있다.

## 부록 A · 논의가 남긴 설계 결론

아홉 스레드 가운데 이 제안의 형태를 실제로 정한 네 묶음이다. §2의 요지 셋이 어디서 나왔는지의 계보이며, 본문을 읽는 데 반드시 필요하지는 않다.

**검토했으나 v0.1에서 후순위·비범위로 둔 항목**부터 밝혀 둔다. 촬영 중 순간 표식, 전사 설정의 기본값 변경과 whisper 자체 VAD, pyannote community-1 실측과 PyTorch 도입, 리뷰 소요시간 지표가 그것이다. 앞의 셋은 화자 판별 경로가 정해지기 전에는 무엇을 기준으로 판정할지가 서지 않고, 마지막 하나는 실제 임상가의 작업 시간을 재는 일이라 모사 자료로 대신할 수 없다. 넷 다 폐기가 아니라 각 스레드의 「남은 질문」으로 남아 있다.

### 제품 가치와 실제 수요 — [001 프로젝트 경쟁력](docs/topics/001_프로젝트_경쟁력/README.md)

무료 로컬 조합과 한국어 클라우드 도구가 이미 있는데 이 도구를 만들 이유가 있는가, 임상가가 실제로 아쉬워하는 시간은 어디인가를 물었다. 판정은 「조건부」였다. 기술 구성요소의 빈틈은 거의 없고 남는 것은 완전 로컬 운용과 대상자 중심 후보 리뷰 흐름이며, 화자 판별 엔진은 제품 정체성이 아니라 **교체 가능한 부품**으로 두라는 권고가 나왔다. 반면 국내·미국 설문이 가리키는 최대 수요는 여전히 전사·분석 자동화였고, 우리가 줄이겠다는 「탐색 시간」만 따로 잰 문헌은 찾지 못했다.

v0.1을 자체 알고리즘 교정이 아니라 경로 비교·선택 단계로 두는 이 제안의 형태가 여기서 나왔다. 동시에 이 제안도 탐색 시간 수요를 입증하지 않는다 — 리뷰 소요시간 지표가 아직 없기 때문이며, 그래서 §7은 임상적 유효성을 주장하지 않는다.

### 실전 실패와 VAD·전사 진단 — [002 첫 실전 실패](docs/topics/002_첫_실전_산출물_실패/README.md) · [004 발성 검출](docs/topics/004_발성_검출_설계/README.md) · [005 전사의 위치](docs/topics/005_전사_기능의_위치/README.md)

첫 실전은 어디서 무너졌는가, 잡음을 통과시키는 합집합을 먼저 조이면 되는가, 전사의 반복 환각은 도구의 한계인가 설정 문제인가를 물었다. 원인 규명은 타스크 #6에서 닫혔다. 합집합은 대상자 발성을 살리는 쪽이어서 먼저 조이면 재현율을 잃고, 참여자와 겹치지 않은 검출에서는 잡음과 표시되지 않은 제3자를 가를 수 없어 오탐을 「리뷰 대상 총량」으로만 보고하기로 했다. 전사의 반복은 5분 실측에서 컨텍스트 캐리오버 탓으로 드러났다 — 최빈 문장 반복이 현행 33.3%에서 `-mc 0`에 25.0%로 내려갔다. **도구를 바꿀 문제가 아니었다.**

그래서 검출 임계 조정과 전사 개선을 v0.1의 첫 행동에서 뺐다(§3·§5). `-mc 0`을 기본값으로 넣는 일과 whisper 자체 VAD 적용은 아직 승인·구현되지 않은 별건이므로 이 제안에 싣지 않았다.

### 화자 판별 경로와 실행 스택 — [003 화자 판별 방식](docs/topics/003_화자_판별_방식/README.md) · [006 실행 스택](docs/topics/006_실행_스택/README.md)

대상자와 임상가를 무엇으로 가를 것인가, 무거운 스택을 들이면 해결되는가를 물었다. 대표 화자 임베딩(ECAPA-TDNN)을 실제로 돌린 결과가 현행보다 나빴고(길이 초과분 +5.6%p → +19.7%p, 무리 분리도 0.866 → 0.399), 문헌에서도 임상·아동 조건의 최고 시스템이 DER 18~26%였다 — 가장 잘하는 기성 도구도 시간의 5분의 1 안팎을 틀린다는 뜻이다. PyTorch는 Python 3.14·Apple Silicon에서 설치되지만(약 1.1 GB·90개 패키지) **도입 근거가 실측·문헌 어느 쪽에도 없었다**. 반대로 임상가 참조 채널은 채널 에너지비와 상호상관으로 numpy 범위 안에서 구현된다. 다만 pyannote community-1은 우리 조건에서 아직 재보지 않았다.

이 제안이 스택 변경을 요구하지 않는 이유, 그리고 기성 파이프라인의 포함 여부만 팀이 따로 정하도록 §7 종료 조건 4에서 갈라 둔 이유가 여기 있다.

### 녹음 조건·제3자 혼입·보조 표식 — [008 옆방 음성 혼입](docs/topics/008_옆방_음성_혼입/README.md) · [009 촬영 중 순간 표식](docs/topics/009_촬영_중_순간_표식/README.md) · [010 현장의 녹화 관행](docs/topics/010_임상_현장의_녹화_관행/README.md)

참여자가 아닌 사람의 말이 섞이는 조건을 어떻게 다루는가, 현장은 회기를 실제로 어떻게 찍는가, 촬영 중 임상가가 남기는 표식이 이 문제를 푸는가를 물었다. 제3자 발성은 잡음이 아니라 말이므로 발성 검출로 걸러지지 않는다. 라벨러에 제3자 범주를 넣었는데도 12.94분에서 제3자 라벨은 **0개**였다 — 없어서가 아니라 경계가 뚜렷하지 않고 횟수가 과다해 사람이 구간으로 끊지 못했기 때문이며, 그 결과 어느 구간이 외부 음성으로부터 깨끗한지도 알 수 없다. 현장 조사에서는 붙박이 단일 원거리 마이크와 관찰실 다채널 설비가 함께 확인됐고 한국 사설 치료실의 분포는 미확인이다. 촬영 중 표식은 사람이 임상 판정을 실시간으로 남기는 별도 채널이지 화자 판별을 풀지 않으며, 표식 기록 자체가 임상 소견이라 금지선 대상이다.

**통제된 모사 자료가 필요한 가장 직접적인 이유가 여기다** — 실제 회기에서는 사람이 만들지 못한 제3자·중첩 정답을 계획된 조건에서만 만들 수 있다. §6 조건표의 제3자 조건과 참여자별 정답 채널, §8의 자료 관리 항목이 그 결과다.
