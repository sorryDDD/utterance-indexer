# 개념 처리 흐름도 — 초안

> **상태**: 프로젝트 루트에 임시로 둔 논의용 초안이다. 승인된 아키텍처나 구현 명세가 아니며, 구체적인 녹음 장비·알고리즘·모델을 결정하지 않는다.

## 목적

이 그림은 임상 회기 기록이 검토 가능한 탐색 인덱스로 바뀌고, 임상가의 검토 결과가 다시 파이프라인 평가에 쓰이는 근본적인 처리 과정을 나타낸다. 각 상자는 특정 구현이 아니라 시스템이 맡아야 할 책임을 뜻한다.

## 흐름도

```mermaid
flowchart TB
    A[/임상 회기 녹화본/]

    subgraph
        direction TB

        B["사람 발성 후보 구간 검출<br/>(누락 최소화에 집중)"]
        C{"누가 낸 발성인가?"}
        D["임상가로 추정된 발성 후보<br/>(텍스트로 전사하여<br/>맥락 파악에 활용)"]
        E["대상자로 추정된 발성 후보<br/>(전사하지 않음)"]
        U["별도 상태로 보존<br/>(중첩/제3자/미확정)"]
        I["탐색 인덱스<br/>모든 후보를 원본 영상 위에 타임스탬프로 표시"]

        B --> C
        C --> D
        C --> E
        C -.-> U
        D --> I
        E --> I
        U --> I
    end

    subgraph
        direction TB

        R["탐색 리뷰어"]
        H(["임상가가 원본을 듣고<br/>판단을 내림"])

        R --> H
    end

    A -->|"오디오"| B
    I --> R
```

1. 임상 회기 녹화본을 입력받는다.
2. 오디오 내의 사람 발성 구간 후보를 누락을 최소화하며(재현율 우선) 검출한다.
3. 검출한 발성 후보가 임상가/대상자 중 누구의 것인지 추정하되, `발성이 겹치는 경우`(중첩), `다른 사람의 소리인 경우`(제3자), `확실하지 않은 경우`(미확정)는 보존한다.
4. 임상가 발화 후보에는 선택적으로 맥락·의미 파악용 전사를 붙이고, 대상자의 것은 전사하지 않는다.
5. 모든 후보를 원본 영상에 연결된 탐색 인덱스로 제공한다.
6. 임상가가 원음을 듣고 실제 발화 여부와 의미를 판단한다.

## 읽는 법

- **로컬 처리 경계** 안의 자료는 외부 서비스로 나가지 않는다.
- **후보 사건 수집**은 최종 발화 판정이 아니다. 놓치면 리뷰 단계에서 복구할 수 없으므로 넓게 수집한다.
- **검토 단위 구성**과 **후보 추정**을 분리한다. 잘못 자른 문제와 잘못 가른 문제를 서로 다른 단계에서 측정하기 위해서다.
- **맥락 정보 부착**은 선택 경로다. 전사는 탐색을 도울 수 있지만 이 프로젝트의 주 기능이나 품질 목표가 아니다.
- 기계가 만드는 것은 어디까지나 후보와 우선순위다. 실제 발화 여부와 임상적 의미는 임상가가 판단한다.
- 검토 결과는 각 단계의 오류를 측정하고 설계를 고치는 근거로 돌아간다. 자동으로 임상 판정을 학습하거나 대체한다는 뜻은 아니다.

## 이 초안이 의도적으로 결정하지 않는 것

- 단일 채널·다중 채널 등 구체적인 녹음 구성
- 임상가 또는 대상자에게 장비를 부착할지 여부
- 발성 검출에 사용할 알고리즘이나 모델
- 화자 또는 후보 관계를 추정하는 구체적인 축과 백엔드
- 전사를 기본으로 실행할지 여부와 전사 모델
- 단계별 임계와 최종 채택 기준

## 초안 검토 질문

1. 각 처리 단계가 구현 수단이 아니라 독립적인 책임으로 나뉘어 있는가?
2. 어느 단계에서 무엇을 버리거나 되돌릴 수 없게 되는지가 드러나는가?
3. 겹침·제3자·미확정 사례를 억지로 두 범주 중 하나로 보내지 않을 자리가 있는가?
4. 입력 수단이나 백엔드가 바뀌어도 나머지 흐름이 유지되는가?
5. 임상가의 판정과 기계의 추정 사이의 경계가 분명한가?
